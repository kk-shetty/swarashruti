[project]
name = "swarashruti"
version = "0.1.0"
description = "Convert hummed audio to instrument playback"
authors = [{name = "Karthik K Shetty", email = "shetty.karthik1857@gmail.com"}]
requires-python = ">=3.11"
dependencies = []   # empty for now

[project.optional-dependencies]
dev = []            # dev tools go here later

[tool.pytest.ini_options]
# pytest config will go here in SWARA-9

[tool.ruff]
# ruff config will go here in SWARA-10